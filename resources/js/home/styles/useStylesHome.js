import { makeStyles } from "@material-ui/core";

export const useStylesHomePage = makeStyles(theme => ({
    root: {
        display: "flex"
    },

    content: {
        flexGrow: 1,
        
    }
}));
